# ÚNICA VPS: Produção + Homologação (staging) no mesmo host
#
# DNS (A record) deve apontar ambos para o IP da VPS:
# - www.netimobiliaria.com.br
# - staging.netimobiliaria.com.br (recomendado)

LETSENCRYPT_EMAIL=admin@netimobiliaria.com.br

# Domínios (usados pelo Caddy para HTTPS)
PROD_DOMAIN=www.netimobiliaria.com.br
STAGING_DOMAIN=staging.netimobiliaria.com.br

# URLs públicas (usadas pelo app)
PROD_APP_URL=https://www.netimobiliaria.com.br
STAGING_APP_URL=https://staging.netimobiliaria.com.br

# Postgres (padronizado)
POSTGRES_IMAGE=postgres:17-alpine
DB_USER=postgres

# Bancos separados (recomendado)
PROD_DB_NAME=net_imobiliaria
STAGING_DB_NAME=net_imobiliaria_staging

# Senhas separadas (recomendado)
PROD_DB_PASSWORD=CHANGE_ME
STAGING_DB_PASSWORD=CHANGE_ME

# Secrets separados (recomendado)
PROD_JWT_SECRET=CHANGE_ME_SUPER_LONG
STAGING_JWT_SECRET=CHANGE_ME_SUPER_LONG
PROD_CRON_SECRET=CHANGE_ME_SUPER_LONG
STAGING_CRON_SECRET=CHANGE_ME_SUPER_LONG

# Opcional (tradução do feed)
LIBRETRANSLATE_URL=https://libretranslate.com


